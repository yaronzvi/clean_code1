import 'package:flutter/material.dart';

class ImportToFirebase extends StatelessWidget {
  const ImportToFirebase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
